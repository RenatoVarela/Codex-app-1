// Voyage AI client — embeddings generation and reranking

// TODO: Initialize Voyage AI client with VOYAGE_API_KEY
// Exports: generateEmbedding(text: string) => Promise<number[]>
// Exports: rerankResults(query: string, documents: string[]) => Promise<RerankResult[]>
export {};
