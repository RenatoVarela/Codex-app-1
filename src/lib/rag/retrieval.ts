// Retrieval — hybrid search combining semantic (pgvector) and full-text (PostgreSQL)

// TODO: Implement hybrid search
// Params: query: string, options?: { topK?: number, semanticWeight?: number }
// Returns: Promise<RetrievedChunk[]>
export {};
